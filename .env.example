# Server
PORT=3000

# Database (PostgreSQL)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=mesh_api

# JWT Authentication
JWT_SECRET=your_jwt_secret_here

# OpenAI (optional — falls back to keyword scoring)
OPENAI_API_KEY=
